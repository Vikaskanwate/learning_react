import { useState } from "react"

export const Form = ({data,setData}) =>{
    const [addData,setAddData] = useState({
        title:"",
        body:""
    })

    return (
        <div security={{}}>

        </div>
    )
}